# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Python dependencies for pucho-telegram-bot
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Async Telegram bot framework (v21+ = async-native, stable)
python-telegram-bot==21.6

# Google Gemini — NEW unified SDK (v1.x), uses X-goog-api-key header,
# supports inline audio without the legacy Files API discovery endpoint.
# Compatible with AQ.-prefixed keys issued by Google AI Studio.
google-genai>=1.0.0

# Environment variable management
python-dotenv==1.0.1

# Async HTTP client — used for ClickUp REST API calls
httpx>=0.27.0

# Async file I/O — used for saving/reading downloaded voice files
aiofiles>=23.2.1
